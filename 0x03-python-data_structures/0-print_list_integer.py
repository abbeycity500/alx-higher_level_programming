#!/usr/bin/python3

# A function that prints all integers of a list
def print_list_integer(my_list=[]):
    # Loop through each elements in my_list
    for i in range(len(my_list)):
        # Print each elements in the list
        print("{:d}".format(my_list[i]))
    
    
